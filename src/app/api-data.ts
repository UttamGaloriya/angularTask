export interface ApiData {
    name: string,
    username: string,
    email: string,
    id: number,
    phone: string,
    website: string
    isEditable?: Boolean
}
export interface TabsData {
    title: string;
    content: string;
}